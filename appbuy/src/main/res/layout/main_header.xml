<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="45dp"
    android:background="@color/black" >

    <ImageView
        android:id="@+id/main_head_back"
        android:layout_width="50dp"
        android:layout_height="45dp"
        android:layout_alignParentLeft="true"
        android:scaleType="centerInside"
        android:layout_centerVertical="true"
        android:src="@drawable/d_back"
        android:visibility="gone" />

    <TextView
        android:id="@+id/main_head_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="代团"
        android:textColor="#BEBEBE"
        android:textSize="19sp"/>

    <ImageView
        android:id="@+id/main_head_unread"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/main_head_share"
        android:layout_marginTop="8dp"
        android:layout_toRightOf="@+id/main_head_title"
        android:contentDescription="未读标识"
        android:src="@drawable/d_unread"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/main_head_share"
        android:layout_width="50dp"
        android:layout_height="45dp"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:scaleType="centerInside"
        android:src="@drawable/d_share_title"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/main_head_delete"
        android:layout_width="50dp"
        android:layout_height="45dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:scaleType="centerInside"
        android:src="@drawable/d_delete"
        android:visibility="gone" />

</RelativeLayout>